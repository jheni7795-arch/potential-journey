import turtle
import math

t = turtle()
t.speed(0)
t.bgcolor("red")
turtle.bgcolor("black")

for haart(n):
    x = 16 * math.sin(n) ** 3
    y = 13 * math.cos(n) - 5 * \
        math.cos(2*n)
        math.cos(4*n)
    return x, y

t..penut()
for i in range(15):
    t.goro(0, 0)
    t.pendown()
    for n in range(0, 100, 2):
        x, y = corazon(n/10)
        t.goto(x*i, y*i)
    t.penop()

t.hideturtle()
turtle.done()
